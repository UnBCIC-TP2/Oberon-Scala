MODULE WriteOnProcedure;
VAR
    x: INTEGER;
PROCEDURE WriteOnProc(VAR x: INTEGER);
BEGIN
    x := 2;
    write(x);
    x := x + 1
END WriteOnProc

BEGIN
    write(x);
    WriteOnProc(x);
    write(x)
END

END WriteOnProcedure.
