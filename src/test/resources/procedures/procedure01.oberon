MODULE SimpleModule;
  
VAR
  x : INTEGER;
  y : INTEGER;

PROCEDURE sum(v1, v2 : INTEGER) : INTEGER;
 BEGIN
   RETURN v1 + v2
 END sum
 
BEGIN
 read(x);
 read(y);
 write(sum(x,y))
END

END SimpleModule.