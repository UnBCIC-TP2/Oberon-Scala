MODULE Fatorial;

VAR
  res : INTEGER;

PROCEDURE fatorial(i: INTEGER) : INTEGER;
 BEGIN
   IF(i = 1)
   THEN
     RETURN 1
   END;
   RETURN i * fatorial(i - 1)
 END fatorial;
 
BEGIN
 res := fatorial(5);
 write(res)
END

END Fatorial.