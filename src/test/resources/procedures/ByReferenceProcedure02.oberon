MODULE increment;

VAR
    x: INTEGER;

PROCEDURE deInc(VAR x: INTEGER);
 BEGIN
    x := x - 1
 END deInc

PROCEDURE inc(VAR x: INTEGER);
 BEGIN
    x := x + 1
 END inc

BEGIN
    x := 2;
    inc(x);
    deInc(x);
    write(x)
END

END increment.
