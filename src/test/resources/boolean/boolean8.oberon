MODULE SimpleModule;

VAR
  x : INTEGER;
  y : INTEGER;
BEGIN
    x := 5;
    y := 12;
    IF (x >= y) THEN
        x := 0
    ELSIF (x < y) THEN
        x := 1
       END
END

END SimpleModule.

MODULE SimpleModule;