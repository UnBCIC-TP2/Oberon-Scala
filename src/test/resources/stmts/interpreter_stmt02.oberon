MODULE SimpleModule;

VAR
  x, z : INTEGER;

BEGIN
    x := 15;
    z := 0;
     
    CASE x OF
        5: x := 1
      | 8: x := x + 80
      | 7: x := x + 2
    ELSE z := 18
    END
END

END SimpleModule.
