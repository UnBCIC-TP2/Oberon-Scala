MODULE SimpleModule;
  
VAR
  x, y, gt : INTEGER;
BEGIN
  readInt(x);
  readInt(y);
  IF(x > y) THEN
    gt := 2
  ELSIF(x < y) THEN
    gt := 1
  ELSE
    gt := 0
  END;
  write(gt)
END

END SimpleModule.
