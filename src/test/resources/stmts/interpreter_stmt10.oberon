MODULE SimpleModule;
  
VAR
  x, y : INTEGER;
BEGIN
  readInt(x);
  IF(x < 18) THEN
    y := x - 18
  ELSIF(x = 18) THEN
    y := 18
  ELSE
    y := x - 18
  END;
  write(y)
END

END SimpleModule.