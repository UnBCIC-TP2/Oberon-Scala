MODULE WhileCaseModule;

VAR
  x, sum, lim : INTEGER;

BEGIN
  x := 0;
  readInt(lim);

  WHILE (x < lim) DO

    CASE x OF
      0:   sum := 0
      ELSE sum := sum + x
    END;

    x := x + 1
  END;

  write(sum)

END

END WhileCaseModule.