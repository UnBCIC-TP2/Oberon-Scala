MODULE LambdaApplication01;

TYPE
   ld = LAMBDA -> (INTEGER, INTEGER): INTEGER
VAR
    x : ld;
    y : INTEGER;

BEGIN
    x := (a: INTEGER, b: INTEGER) => a + b;
    y := x.apply(1,2)
END

END LambdaApplication01.
