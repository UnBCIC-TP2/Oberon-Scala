MODULE LambdaExpressionsIT04;

TYPE
   ld = LAMBDA -> (BOOLEAN): BOOLEAN
VAR
    x : ld;
    r : BOOLEAN;

BEGIN
    x := (a: BOOLEAN) => a || false;
    r := (x)(true)
END

END LambdaExpressionsIT04.
