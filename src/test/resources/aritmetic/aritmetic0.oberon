MODULE SimpleModule;

VAR
  a : REAL;
  b : INTEGER;
  c : INTEGER;
  d : REAL;
  e : INTEGER;

  f : INTEGER;
  g : REAL;
  h : INTEGER;
  i : REAL;
  j : INTEGER;


  k : INTEGER;
  l : REAL;
  m : INTEGER;
  n : INTEGER;
  o : REAL;

  x : INTEGER;
  y : INTEGER;
  z : INTEGER;
  w : REAL;
  t : REAL;

BEGIN
    a := 1.5;
    b := 2;
    c := 3;
    d := 4.5;
    e := 5;
    f := 6;
    g := 7.5;
    h := 8;
    i := 9.5;
    j := 10;
    k := 11;
    l := 12.5;
    m := 13;
    n := 14;
    o := 15.5;

    x := c + j + k;
    y := b + h + n; 
    z := e + f + m; 
    w := a + g + l;
    t := d + i + o 
END

END SimpleModule.
