MODULE CFunction;

CONST
  z = 5 * 10;

VAR
  abc : INTEGER;
  gui : BOOLEAN;

FOREIGN IMPORT abs(a: INTEGER): INTEGER;
FOREIGN IMPORT pow(a, b: INTEGER, INTEGER): INTEGER;
FOREIGN IMPORT powb(a, b: INTEGER, BOOLEAN): INTEGER

END CFunction.