MODULE Ffi;
  
VAR
  a : INTEGER;
  b : INTEGER;
  ansa : INTEGER;
  ansb : INTEGER;

FOREIGN IMPORT abs(n: INTEGER): INTEGER;

BEGIN
 readInt(a);
 readInt(b);

 ansa := abs(a)
 ansb := abs(b)
END

END Ffi.