MODULE Ffi;

VAR
  a : INTEGER;
  b : INTEGER;
  ans : INTEGER;

FOREIGN IMPORT abs(n: INTEGER): INTEGER;
FOREIGN IMPORT isalnum(n: INTEGER): INTEGER;

BEGIN
 a := 65;
 IF (isalnum(a) = 0) THEN
    a := 10;
 ELSE
    a := (1 - 2);
 END

 ans := abs(a);
END

END Ffi.