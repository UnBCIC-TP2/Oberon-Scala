MODULE Ffi;

VAR
  a : INTEGER;
  b : INTEGER;
  ans : INTEGER;

FOREIGN IMPORT isdigit(n: INTEGER): INTEGER;
FOREIGN IMPORT abs(n: INTEGER): INTEGER;

BEGIN
 a := 5 + 4;
 b := 1 + 0;
 ans := a + b;
 ans := isdigit(a);
 ans := abs(ans);
END

END Ffi.