#include <stdio.h>
#include <stdbool.h>


int x, y, z, k;


int main() {
    scanf("%d", &y);
    x = 0;
    while (x < y) {
        z = 0;
        while (z < y) {
            k = z + x;
        }
    }
    printf("%d\n", k);
}