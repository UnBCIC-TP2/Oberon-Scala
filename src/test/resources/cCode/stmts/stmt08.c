#include <stdio.h>
#include <stdbool.h>

int main() {
    int x, y, z, k;

    scanf("%d", &y);
    x = 0;
    while (x < y) {
        z = 0;
        while (z < y) {
            k = z + x;
        }
    }
    printf("%d\n", k);
}