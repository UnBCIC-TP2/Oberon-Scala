#include <math.h>
#include <stdio.h>
#include <stdbool.h>
#include <stdlib.h>

struct HALLS_struct {
    int matricula;
    bool integrante;
};
typedef struct HALLS_struct HALLS;
typedef HALLS HALLS_array[9];

int x;


int main() {
    scanf("%d", &x);
}