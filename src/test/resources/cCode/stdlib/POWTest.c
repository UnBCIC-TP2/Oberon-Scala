#include <math.h>
#include <stdio.h>
#include <stdbool.h>
#include <stdlib.h>


float x, y, z, w, a, b;


int main() {
    x = 2.5;
    y = -1.5;
    a = -9.0;
    b = 3.0;
    z = pow(x, y);
    w = pow(a, b);
}
